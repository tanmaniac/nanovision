# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ipynb_checkpoints/

# venvs
.venv/
venv/

# assignment outputs (viz renders, logs, checkpoints)
assignments/**/out/
**/runs/
*.ckpt
*.pt

# datasets (nuScenes etc. — never commit). Anchored to repo root so the
# nanovision/data/ package is NOT ignored.
/data/
/datasets/
*.zip

# os
.DS_Store
